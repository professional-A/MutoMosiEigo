# 仕様書：武藤模試 v2「エンジン化」計画 v2.1（学習科学統合版）

> 本書は v2.0 を全面改訂し置き換える。変更の根拠は `docs/research/learning-science.md`（学習科学・Claude Codeベストプラクティスのリサーチ報告）。
> 実装者はClaude Code上のClaude Sonnet。プランモードで設計確認→マイルストーン順に実装。各マイルストーン完了時に `npm run dev` で動作確認しコミット。曖昧点は最も単純な解釈で実装し `// TODO(仕様確認):` を残す。マイルストーン完了ごとに /clear 推奨。

---

## 0. 背景と目的

「武藤模試」は作者（高専4年）が科目ごとに配布してきた試験対策HTMLクイズ。v2はこれを**認知科学のエビデンスに基づく学習エンジン**に引き上げる。

**設計の科学的土台（詳細と出典は docs/research/ 参照）**：
- 検索練習（テスト効果）と分散学習は、学習技法の中で最高評価（Dunlosky et al. 2013; Roediger & Karpicke 2006; Cepeda et al. 2006）
- Leitner箱式＝successive relearning（正解するまで→後日また正解するまで）の自動化（Rawson & Dunlosky 2022）
- 工学系科目には worked examples 効果（Sweller & Cooper 1985）と交互学習（Rohrer）

| 課題 | 解決 |
|---|---|
| ① 質・網羅性が本番に追いつかない | 作問パイプライン（多段プロンプト＋カバレッジ監査＋過去問スタイル注入） |
| ② 解いて終わりで定着しない | 復習システム（Leitner＋試験日逆算スケジューラ＋習熟ダッシュボード） |
| ③ 量産できない | エンジン化（JSON差し替え＋スタンドアロンHTML書き出し＋mutomoshiスキル） |

対象6教科：英語（科学技術英語）・応用数学・応用物理・熱流体工学・加工学・制御工学。
目標：新科目の武藤模試を「ノート用意から30分以内」で配布可能に。

**成果物**：A=エンジン（Webアプリ）、B=作問パイプライン（prompts/＋mutomoshiスキル。販売素材のため自己完結した書き方で）。
**制約**：運用費ゼロ。BYO-AI方式（エンジンはAI APIを呼ばない）。完全クライアントサイド。

---

## 1. 技術スタックとリポジトリ構成

Vite + React 18 + TypeScript / Tailwind / KaTeX（npm同梱・CDN不使用）/ lz-string / localStorage（schemaVersion付きマイグレーション必須）/ Vitest / Cloudflare Pages

```
mutomoshi/
├── CLAUDE.md               # ~200行以内厳守（→§9）
├── .mcp.json               # Context7（git管理、シークレット書かない）
├── .claude/
│   ├── settings.json       # 権限: npm run * =allow / git push =ask / rm -rf,.env =deny
│   │                       # hooks: pre-commit で typecheck && lint && test
│   ├── commands/
│   └── skills/mutomoshi/   # SKILL.md(500行以内)+references/+scripts/validate.py
├── docs/
│   ├── specs/              # 本仕様書
│   └── research/           # learning-science.md（リサーチ報告）
├── src/
│   ├── components/  ├── pages/  ├── hooks/
│   ├── lib/          # leitner.ts, scheduler.ts, jsonRepair.ts, storage.ts,
│   │                 # share.ts, promptBuilder.ts, standaloneExport.ts, packMigrate.ts
│   ├── types/quiz.ts
│   ├── data/         # 教科別問題JSON（サンプル）
│   └── config.ts     # 支援リンク等
├── prompts/          # P1〜P4（販売素材）
└── tests/
```

---

## 2. データスキーマ（問題パック v2）

```json
{
  "version": 2,
  "subject": "応用物理",
  "title": "武藤模試 応用物理全網羅編",
  "examDate": "2026-09-28",
  "categories": ["剛体", "波動"],
  "questions": [
    {
      "id": "rigid-001",
      "type": "mc",
      "category": "剛体",
      "difficulty": 2,
      "source": "ノートp.3",
      "question": "半径 $r$、質量 $M$ の一様な円板の中心軸まわりの慣性モーメントは？",
      "choices": ["$Mr^2$", "$\\frac{1}{2}Mr^2$", "$\\frac{2}{5}Mr^2$", "$\\frac{1}{3}Mr^2$"],
      "answer": 1,
      "explanation": "円板は $I=\\frac{1}{2}Mr^2$。球（$\\frac{2}{5}Mr^2$）との混同に注意。",
      "workedExample": {
        "problem": "半径 $r$、質量 $M$ の一様な円柱の慣性モーメントを求めよ。",
        "solution": "微小リングに分割し $dI = r'^2 dm$ を積分する。…（解答手順）"
      }
    },
    {
      "id": "wave-003",
      "type": "steps",
      "category": "波動",
      "title": "波動方程式の導出",
      "intro": "微小要素に働く張力から導く。",
      "steps": [
        { "text": "微小要素の質量は $dm = {{blank}}$", "answer": "\\rho dx",
          "why": "線密度×長さ。なぜ面密度でないか自分で説明してみよう" }
      ],
      "explanation": "..."
    }
  ]
}
```

型ルール（`src/types/quiz.ts`）：
- `type`: `mc`（4択、answer=0始まりindex、表示時シャッフル＆再マップ）/ `fill`（{{blank}}）/ `qa`（記述・自己採点）/ `steps`（大問ステップ穴埋め）
- fill/steps判定：trim＋全半角正規化一致→正解。不一致は模範解答表示→「合ってた／間違えた」自己採点（表記ゆれ対策）
- `id`：文字列必須・パック内一意（学習記録キー。再生成でも維持するようプロンプト側で指示）
- **`workedExample`（任意）**：解説付き例題。学習モードで問題の前に「例題を読む」→本問（類題）の順で表示（worked example効果。模試モードでは非表示）
- **`why`（steps内・任意）**：自己説明を促す一言。解答後に表示
- **`examDate`（任意）**：試験日。スケジューラが使用
- 旧v1スキーマは読込時に自動変換。未知フィールド無視、未知typeスキップ＋警告

---

## 3. 機能要件

### 3.1 プレイヤー・出題モード
- **F1 パック管理（ホーム）**：一覧＋習熟サマリ。JSON貼付/ファイル読込。同titleの再取込は「更新」でid一致の学習記録を引き継ぐ
- **F2 JSON取込＋自動修復**：①コードブロック記号除去 ②最初の`{`〜最後の`}`抽出 ③末尾カンマ除去 ④全角引用符の慎重な正規化。修復不能時はエラー位置＋「AIに『JSONだけ出力して』と返信」案内。**Vitestテスト必須**
- **F3 出題モード**（学習科学の使い分けをUIで自然に誘導）：
  - **今日の復習**（デフォルト・最優先表示）：スケジューラ（F7）が選んだ「今日やるべき問題」
  - **新規学習**：未出題の問題。workedExampleがあれば例題→類題の順
  - **先解きモード（pretesting）**：新単元を「まず解く（間違えてOK）→即解説」。開始時に「間違えるほど覚える、が科学的に正しい」と一言表示
  - **カテゴリ別**／**ミックス演習**（カテゴリ横断シャッフル＝交互学習。「本番はどの解法か自分で選ぶ練習」と説明）
  - **模試モード**：ランダムN問＋タイマー、workedExample非表示、採点は最後にまとめて
- **F4 プレイ画面**：1問1画面＋進捗バー。解答表示前に確認ステップ（誤タップ防止）。即時フィードバック（正誤＋短い解説を必ず表示）。KaTeX描画、横長数式は横スクロール
- **F5 結果画面**：正答率・所要時間・カテゴリ別内訳・間違い一覧・「間違いだけ再挑戦」・**「結果をコピー」**（テキスト要約→LINE共有兼データ収集）

### 3.2 復習システム（コア）
- **F6 学習記録**：問題idごとに履歴（正誤・日時・箱）。localStorage、schemaVersion付き
- **F7 Leitner 3箱＋試験日逆算スケジューラ**：
  - 箱0（新規/苦手）→箱1（復習中）→箱2（習得）。**正解で1つ昇格、不正解は必ず箱0へ降格**（このルールの厳守がsuccessive relearningの要）
  - 復習間隔の初期値：箱0=当日〜翌日、箱1=2〜3日、箱2=7日
  - `examDate`があれば間隔を自動調整：**目安は「試験までの残り日数の10〜30%」**（Cepeda et al. 2008の知見）。試験3日前からは「総仕上げ」として箱2含む全問1周を提案
  - 「今日の復習」の出題順：箱0優先→箱1→（設定ONなら箱2）
  - **出題の難易度バランス**：直近の正答率が85%を大きく超えたら難問（difficulty高・箱0滞留問題）を優先混入、大きく下回ったら易問と例題を混ぜる（85%ルールを設計参考として使用。UIでは「ちょうどいい難しさに調整中」程度の表現に留める）
  - **leitner.ts / scheduler.ts はVitestテスト必須**（昇降・間隔計算・試験直前モード）
- **F8 習熟ダッシュボード**：カテゴリ×習熟度グリッド、箱の分布（新規/復習中/習得の件数）、**試験日カウントダウンと「今日やるべき問題数」**、学習日の連続記録（ストリーク）
  - **ストリークには免除機構必須**：週1回まで休んでも途切れない「おやすみ券」。リーダーボードは実装しない
- **F9 成績エクスポート**：学習記録CSV書き出し（効果検証レポート用）

### 3.3 共有・配布
- **F10 共有URL**：lz-string圧縮で`#/play?q=...`。8,000文字超で自動無効化→F11へ誘導
- **F11 スタンドアロンHTML書き出し**：パック埋め込みの単一HTML（KaTeX同梱・完全オフライン・〜2MB許容）。プレイ＋復習が単体で動く。LINE配布フローの後継
- **F12 作問ウィザード**：科目・カテゴリ・問題数・形式配分・難易度→P2プロンプト組立→コピー→「Claude/ChatGPT/Geminiを開く」リンク

### 3.4 作問パイプライン（成果物B、prompts/）
- **P1 トピック抽出**：ノート→カテゴリ・トピック・重要度のJSON
- **P2 作問（本体）**：スキーマ厳守／形式内訳／難易度定義／「ノートにない内容は出題しない」／**「誤答選択肢はもっともらしく＝正解と混同しやすいものにする」**（competitive alternatives、Little et al. 2012）／「idは`カテゴリ略称-連番`で再生成時も維持」／解説1〜2文必須／**計算・導出問題にはworkedExample（例題＋解答手順）を付ける**
- **P3 カバレッジ監査**：ノート＋生成済みパック→「カバーしていない内容を列挙→追加作問」。網羅性はこの2周目で担保
- **P4 過去問スタイル注入**：過去問→出題傾向の特徴抽出→P2への追加指示文を生成
- **教科プリセット**（P2に同梱する追加指示テンプレ）：
  - 語彙・用語系（英単語/熟語、各教科の専門用語）：mc（紛らわしい誤選択肢）＋fill中心
  - 問題解決系（応用数学・応用物理・熱流体・加工学・制御）：steps＋workedExample中心、fill/qaで生成を強制
  - **英語**：4step勉強法の改良版を反映——「文法の骨格を早めに→単語・熟語は分散並行→本文はpretestingしてから」の順でカテゴリ構成を指示
- **Claude Code用スキル `mutomoshi`**（`.claude/skills/mutomoshi/`）：SKILL.md（500行以内、descriptionに「何を・いつ」明記：例「授業ノートや教材から武藤模試の問題パックJSONを生成する。『模試を作って』『ノートから問題作って』と言われたら使う」）。詳細テンプレは`references/`、スキーマ検証は`scripts/validate.py`に分離。ノート→P1→P2→P3→検証→パックJSON＋スタンドアロンHTMLまで一気に出力する手順を記述

MVP外（P1以降）：画像問題／FSRS等の本格SRS／PWA／多端末同期。

---

## 4. 画面構成

1. **ホーム**：「今日の復習：N問（試験まであとX日）」を最上部に→ダッシュボード→パック一覧→「新しい模試を作る」
2. **作成**：ウィザード（F12）→JSON取込（F2）のステップUI
3. **モード選択→プレイ→結果**
4. 未取込ユーザー向けLP要素：3ステップ図解＋「検索練習×分散学習という、認知科学で最も効果が高いと評価された学習法に基づく設計（出典つき）」＋実績「模試点と本番点に強い相関（r≈0.95、n=5・参考値）」——**誇張禁止：「必ず上がる」等はNG、nと参考値を必ず併記**
5. フッター：「作者を支援」「プロンプト全集」リンク枠（config.ts、初期は非表示フラグ）。広告なし

---

## 5. デザイン方針

テーマ「試験前夜の日本の学生の机」。方眼ルーズリーフ×蛍光マーカー×**赤シート**。
- シグネチャー（1点豪華）：解答・解説は赤シート風半透明カバーの下、タップ/スワイプでスッと退く。アニメはここだけ
- パレット：紙白#FAFAF7／罫線ブルーグレー#B8C4D9／インク#2B2E3A／赤シート#E8443A／マーカーグリーン#3DA35D。汎用AI風のダーク＋蛍光は避ける
- 見出しZen Kaku Gothic New、本文システムフォント。背景にCSS方眼
- 品質フロア：366px幅で崩れない／フォーカス可視／prefers-reduced-motionでシート演出無効

## 6. 非機能要件
- 外部通信ゼロ（フォント除く）。入れるならCloudflare Web Analytics（Cookieレス）のみ
- localStorage破壊的変更禁止（マイグレーション必須）
- UI文言・コメントは日本語。学習科学の説明文はUI内では1行以内の平易な表現に

## 7. マイルストーンと受け入れ基準

| M | 内容 | 受け入れ基準 |
|---|---|---|
| M1 | 型定義＋プレイヤー（4形式＋workedExample＋KaTeX） | 全形式が解答→結果まで通る。例題→類題の順で表示される |
| M2 | 復習システム（F6〜F8）＋leitner/schedulerテスト | 間違い→箱0→2回正解で習得。examDateで間隔が変わる。更新後も記録が残る |
| M3 | 取込・修復・v1変換・パック管理＋テスト | 壊れたJSONで親切なエラー。更新で記録引き継ぎ |
| M4 | 共有URL＋スタンドアロンHTML | 機内モードで完全動作（数式含む）。別ブラウザでURL共有可 |
| M5 | ウィザード＋prompts/一式＋mutomoshiスキル＋validate.py | P2を実際にAIに貼って返ったJSONがそのまま取り込める（手動E2E）。validate.pyがスキーマ違反を検出 |
| M6 | ホームLP・デザイン仕上げ・README・hooks・デプロイ | スマホ実機相当で全画面確認。pre-commitでtest失敗時にコミット拒否 |

## 8. 開発運用ルール（Claude Code）
- 非自明なタスクはプランモードから。1タスク1コミット
- マイルストーン完了ごとに`/clear`、同一機能の作り込み中は`/compact`
- ライブラリAPIはContext7で最新を確認（React/Vite/KaTeX/lz-string）
- 「できた」は証拠（テスト出力・スクリーンショット）とセットで報告

## 9. CLAUDE.md に書く内容（別ファイルとして作成、~200行以内）
①1行説明「試験対策クイズエンジン。学習科学ベース。詳細は docs/specs/」②スタック ③コマンド（dev/build/test/単一テスト/lint/typecheck、コミット前にtypecheck&lint必須）④主要ディレクトリ3〜5個 ⑤禁止事項（localStorage破壊的変更・外部API呼び出し・prompts/の仕様変更は要確認）⑥用語（パック/箱/模試モード）。タスク固有の指示・コード片・時限情報は書かない。

## 10. 収益素材の扱い
- prompts/とskills/は自己完結した文章で（単体販売可能な体裁）。LICENSE同梱、有料版は再配布禁止を明記
- スキル本体は無料GitHub配布で集客、有料はプロンプト集・教科別テンプレ・解説note（Claude Codeにスキル課金機構はないため）
- 宣伝で使ってよい表現／NG表現の線引きは docs/research/learning-science.md の該当節に従う
